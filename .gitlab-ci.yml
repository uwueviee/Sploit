image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir public
    - mkdir public/documentation
    - cp -rp Documentation public/documentation
  artifacts:
    paths:
      - public
  only:
    - master
