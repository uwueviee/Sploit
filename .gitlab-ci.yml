image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir public
    - cp -rp Site public
    - mkdir public/documentation
    - cp -rp Documentation public/documentation
  artifacts:
    paths:
      - public
  only:
    - master
